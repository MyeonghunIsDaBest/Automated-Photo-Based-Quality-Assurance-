import { DailyLog } from '../types';

// Empty seed — daily logs will be authored against the live pilot project.
// The Logs tab on the Projects page will render its empty state until entries
// are recorded.
export const mockDailyLogs: DailyLog[] = [];
