import { ProjectDocument } from '../types';

// Empty seed — uploads from the live pilot will populate this list via the
// Upload + Files flows. Until backend wiring lands, the Documents tab will
// surface its empty state.
export const mockDocuments: ProjectDocument[] = [];
