// Writing-assist primitives barrel. Future surfaces (Punch / Reports /
// incident notes) import from here so the source layout can move without
// touching every call site.
export { default as WritingAssistButton } from './WritingAssistButton';
export { default } from './WritingAssistButton';
